mobile_robot_runtime_monitor:
  ros__parameters:
    publish_rate_hz: 2.0
    joint_states_timeout_sec: 1.0
    odom_timeout_sec: 1.0
    joint_states_topic: /joint_states
    odom_topic: /diff_drive_controller/odom
